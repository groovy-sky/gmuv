name: Generate a report with gmuv

on:
  push
jobs:
  run-gmuv:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v3

      - name: Generate report for a test repository
        uses: groovy-sky/gmuv@v1.1.0
        with:
          account: 'groovy-sky'
          repository: 'aaa'

      - name: Publishing the changes
        run: ls